export default () => ({
    'strapi-v5-plugin-populate-deep': {
        config: {
            defaultDepth: 5, // Default is 5
        }
    },
    'media-prefix': {
        enabled: true,
    },
});
